[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[functions."push-scheduler"]
  schedule = "0 * * * *"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
